export interface Stats {
  avgScore: number;
  maxScore: number;
  avgGuesses: number;
  maxGuesses: number;
  avgPlaysPerDay: number;
  maxPlaysPerDay: number;
  avgDuration: number;
  maxDuration: number;
}
