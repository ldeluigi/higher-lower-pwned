import { HistoryItemToStartDatePipe } from './history-item-to-start-date.pipe';

describe('HistoryItemToStartDatePipe', () => {
  it('create an instance', () => {
    const pipe = new HistoryItemToStartDatePipe();
    expect(pipe).toBeTruthy();
  });
});
