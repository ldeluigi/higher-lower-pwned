import { HistoryItemToEndDatePipe } from './history-item-to-end-date.pipe';

describe('HistoryItemToEndDatePipe', () => {
  it('create an instance', () => {
    const pipe = new HistoryItemToEndDatePipe();
    expect(pipe).toBeTruthy();
  });
});
